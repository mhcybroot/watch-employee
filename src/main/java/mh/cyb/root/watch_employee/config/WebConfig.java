package mh.cyb.root.watch_employee.config;

import org.springframework.context.annotation.Configuration;

/**
 * Web MVC configuration.
 * CORS is handled centrally in SecurityConfig to avoid conflicting
 * configurations.
 */
@Configuration
public class WebConfig {
    // CORS configuration has been consolidated in SecurityConfig.java
}
